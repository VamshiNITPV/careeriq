# CareerIQ backend — development and test dependencies.
# Never installed into the production image (see Dockerfile stages).

# --- testing ---
pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=6.0
# httpx powers FastAPI's ASGI test transport — requests never touch the network.
httpx>=0.28

# --- quality ---
ruff>=0.8
mypy>=1.13

# --- test fixtures ---
# Generates real PDFs for the parser tests. Hand-crafted PDF bytes would test
# our own byte-writing rather than the extraction path a real file exercises.
fpdf2>=2.8

# --- typing stubs ---
types-redis>=4.6
