# CareerIQ backend — development and test dependencies.
# Never installed into the production image (see Dockerfile stages).

# --- testing ---
pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=6.0
# httpx is a runtime dependency now (requirements.txt). Kept listed here
# because the test suite uses it directly, as FastAPI's ASGI transport and
# as MockTransport for the jobs adapter — neither touches the network.
httpx>=0.28

# --- quality ---
ruff>=0.8
mypy>=1.13

# --- test fixtures ---
# fpdf2 was here, for generating real PDFs in the parser tests. It moved to
# requirements.txt when resume tailoring started producing PDFs: the production
# image installs that file alone, so a production code path cannot depend on
# anything listed only here.

# --- typing stubs ---
types-redis>=4.6
