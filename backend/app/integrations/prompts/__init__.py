"""Versioned prompt templates (ml.md section 6.2).

Prompts live here rather than inline in services because a prompt change alters
output quality, and an unversioned change is an unreproducible regression.
"""
